Use a library like shell-escape to escape user input, e.g., const shellEscape = require('shell-escape'); ... execSync(`az login --service-principal -u ${shellEscape([servicePrincipalId])} -p ${shellEscape([servicePrincipalKey])} --tenant ${shellEscape([tenantId])}`)